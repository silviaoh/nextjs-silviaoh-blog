import { DefaultTheme } from 'styled-components';

const fontSize = {};

const colors = {
  black: '#000',
  gray: '#F8F8F8',
  darkGray: '#a9a9a9',
  deepDarkGray: '#6e6e6e',
  purple: '#94A7DF',
};

const theme: DefaultTheme = {
  colors,
  fontSize,
};

export default theme;
