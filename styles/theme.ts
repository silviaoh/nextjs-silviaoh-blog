import { DefaultTheme } from 'styled-components';

const fontSize = {};

const colors = {
  black: '#000',
  gray: '#E6E7E8',
  darkGray: '#a9a9a9',
  purple: '#af69ef',
};

const theme: DefaultTheme = {
  colors,
  fontSize,
};

export default theme;
