import React from 'react';
import PrimaryLayout from '../components/layouts/PrimaryLayout';

const BlogView = (props: any) => {
  return <PrimaryLayout {...props}>BlogView</PrimaryLayout>;
};

export default BlogView;
